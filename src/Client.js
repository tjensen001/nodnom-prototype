import React, { Component } from  'react';
import Router from 'react-router';


//Main component that we'll use to setup routes and render different components
class Client extends Component {
    render(){
        return (
            <div>
                {"Hello world"}
            </div>
        );
    }
}

export default Client;